FROM openjdk:17
WORKDIR /app
COPY ./target/*.jar app.jar
EXPOSE 8085

ENV RABBITMQ_PORT=5672
ENV RABBITMQ_QUEUE=uptade-response-email
ENV RABBITMQ_HOST=rabbitmq
ENV RABBITMQ_USERNAME=brito
ENV RABBITMQ_PASSWORD=1234567

ENTRYPOINT ["java", "-jar", "app.jar"]
